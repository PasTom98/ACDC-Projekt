{
    // name of Container
    "name": "devcontainer-app",
  
    "dockerComposeFile": "../../docker-compose.dev.yaml",
    "service": "app",
    "workspaceFolder": "/var/www/html",
  
    "postCreateCommand": "apt install composer"
}